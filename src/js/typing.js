const Typed = require('typed.js');

const type = new Typed('#typed', { // eslint-disable-line no-unused-vars
  stringsElement: '#typed-strings',
  smartBackspace: true,
  loop: true,
  typeSpeed: 20,
});
